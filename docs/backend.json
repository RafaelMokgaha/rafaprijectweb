{
  "entities": {
    "GameRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "GameRequest",
      "type": "object",
      "description": "Represents a user's request for a game on the RAFA Project platform.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Game Request."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who made the request."
        },
        "gameName": {
          "type": "string",
          "description": "The name of the game requested by the user."
        },
        "platform": {
          "type": "string",
          "description": "The platform for which the game is requested (e.g., PC, PS4/5, Xbox, Switch)."
        },
        "name": {
          "type": "string",
          "description": "The name of the user submitting the request."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user submitting the request.",
          "format": "email"
        },
        "notes": {
          "type": "string",
          "description": "Additional notes or comments provided by the user with the request."
        },
        "requestDate": {
          "type": "string",
          "description": "The date and time when the game request was submitted.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The status of the request (e.g., pending, approved, denied)."
        }
      },
      "required": [
        "id",
        "userId",
        "gameName",
        "platform",
        "name",
        "email",
        "requestDate",
        "status"
      ]
    },
    "AvailableGame": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AvailableGame",
      "type": "object",
      "description": "Represents a game available on the RAFA Project platform.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the available game."
        },
        "name": {
          "type": "string",
          "description": "The name of the available game."
        },
        "supportedPlatforms": {
          "type": "array",
          "description": "A list of platforms on which the game is supported.",
          "items": {
            "type": "string"
          }
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the image representing the game."
        }
      },
      "required": [
        "id",
        "name",
        "supportedPlatforms",
        "imageUrl"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the RAFA Project platform.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "name": {
          "type": "string",
          "description": "The name of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "registrationDate": {
          "type": "string",
          "description": "The date and time when the user registered.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "registrationDate"
      ]
    },
    "Message": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Message",
      "type": "object",
      "description": "Represents a message sent from an admin to a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the message."
        },
        "receiverId": {
          "type": "string",
          "description": "The UID of the user receiving the message."
        },
        "subject": {
          "type": "string",
          "description": "The subject of the message."
        },
        "body": {
          "type": "string",
          "description": "The content of the message."
        },
        "sentAt": {
          "type": "string",
          "description": "The date and time when the message was sent.",
          "format": "date-time"
        },
        "isRead": {
          "type": "boolean",
          "description": "Whether the user has read the message."
        },
        "attachmentUrl": {
          "type": "string",
          "description": "URL of the attached file in Firebase Storage."
        },
        "attachmentName": {
          "type": "string",
          "description": "The original name of the attached file."
        }
      },
      "required": [
        "receiverId",
        "subject",
        "body",
        "sentAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. Uses path-based ownership for private user data. User id is the firebase auth uid",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user, corresponding to their Firebase Auth UID."
            }
          ]
        }
      },
      {
        "path": "/game_requests/{gameRequestId}",
        "definition": {
          "entityName": "GameRequest",
          "schema": {
            "$ref": "#/backend/entities/GameRequest"
          },
          "description": "Stores user game requests. Each document represents a single request.",
          "params": [
            {
              "name": "gameRequestId",
              "description": "The unique identifier of the game request."
            }
          ]
        }
      },
      {
        "path": "/available_games/{availableGameId}",
        "definition": {
          "entityName": "AvailableGame",
          "schema": {
            "$ref": "#/backend/entities/AvailableGame"
          },
          "description": "Stores information about available games on the platform. Each document represents a single available game.",
          "params": [
            {
              "name": "availableGameId",
              "description": "The unique identifier of the available game."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/messages/{messageId}",
        "definition": {
          "entityName": "Message",
          "schema": {
            "$ref": "#/backend/entities/Message"
          },
          "description": "Stores messages sent to a specific user. This is a sub-collection of the users collection.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns this message inbox."
            },
            {
              "name": "messageId",
              "description": "The unique identifier of the message."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to store game requests, available games, user data and messages for the RAFA Project Launcher. It prioritizes simplicity and security. User data is stored in a private collection using path-based ownership. Messages are stored as a subcollection under each user to enforce privacy. Game requests are stored in a flat collection. Available games are stored in a separate flat collection.\n\nAuthorization Independence:\nThere is no need to denormalize any data.\n\nQAPs:\nList operations are secured by structural segregation."
  }
}
